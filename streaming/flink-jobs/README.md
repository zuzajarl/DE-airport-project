# Flink Jobs

This module should contain the streaming logic for:

- parsing raw Kafka events
- deduplicating repeated polls
- detecting status changes
- calculating delay in minutes
- writing curated outputs to sinks
