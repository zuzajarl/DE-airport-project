# Producer

This module should contain the polling service that calls the AeroDataBox API for Krakow Airport arrivals and publishes raw events to Kafka.

Recommended files:

- `producet.py`: AeroDataBox API client
- `publisher.py`: Kafka producer

